import { AbstractSettingDirective } from './abstract-setting.directive';

describe('AbstractSettingDirective', () => {
  it('should create an instance', () => {
    const directive = new AbstractSettingDirective();
    expect(directive).toBeTruthy();
  });
});
