import { AbstractControlDirective } from './abstract-control.directive';

describe('AbstractControlDirective', () => {
  it('should create an instance', () => {
    const directive = new AbstractControlDirective();
    expect(directive).toBeTruthy();
  });
});
